<h1>I am user index page</h1>
    <table>
        <tr>
        <th>我是</th>
        </tr>
    <% @users.each do |u| %>
    <tr>
      <td><%= u.username %></td>
      <td><%= link_to 'Show', user_path(u) %></td>
    </tr>
    <% end %>
    </table>
<%= link_to "Sign up", register_path(:new) %>